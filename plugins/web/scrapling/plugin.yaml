name: web-scrapling
version: 1.0.0
description: "Scrapling local web extraction provider. Uses the installed scrapling Python package for no-key static page extraction."
author: NousResearch
kind: backend
provides_web_providers:
  - scrapling
